<%- include("includes/head.ejs"); %>

    <body>
        <h1>Requist to <%= data.name %>
        </h1>
        <button>Follow</button>
        <button>Following</button>
        <br>
        <!--<ul>
        <% //for(user of followers) { %>
            <li>
                <%= //user %>
            </li>
            <% //} %>
    </ul>-->
        <hr>
        <% for(let i of data.posts) { %>
            <img src="<%=i.image %>">
            <%}%>

    </body>

    </html>